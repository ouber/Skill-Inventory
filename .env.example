# MongoDB connection (MongoDB Atlas)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/?appName=Cluster0
MONGODB_DB=skill_inventory

# Optional: simple token to protect admin write endpoints (leave empty to disable)
# When set, admin POST/PUT/DELETE requests must include header: x-admin-token: <value>
ADMIN_TOKEN=
